class Main
{
	static void run()
	{
		String str 	  = "        a      \041           b    \041     c";
		String substr = "\041";
		int index = ifj16.find(str, substr);
		if (index == 15)
		{
			ifj16.print("ifj16.find!\n");
		}
		else
		{
		}
	}
}
